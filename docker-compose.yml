version: "3.9"

services:

  postgres:
    image: postgres:17
    container_name: openhealth-postgres
    env_file:
      - .env.docker
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: